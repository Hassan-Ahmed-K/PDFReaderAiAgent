services:
  - type: web
    name: pdf-reader-fastapi
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/backend/main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.14.3
